//multiply number until

function persistence(num) {
  let numstring = [];
  count = 0;
  numstring = num.toString().split("");
  //map conversion from string to digits
  console.log(numstring);
}

persistence(3456779);
